# Local datasets

No dataset has been selected or downloaded yet. Store raw assets in `raw/` and
processed assets in `processed/`; both are ignored by Git. Before training,
record the source, version, license, class mapping and train/validation/test split.
Required soccer labels are player, goalkeeper, referee and ball.
